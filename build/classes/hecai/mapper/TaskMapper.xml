<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hecai.mapper.TaskMapper">
	
	<insert id="addTask" parameterType="string">
		insert into tb_test values(#{test, javaType=String,jdbcType=Date})
<!-- 		insert into tb_task values( -->
<!-- 			null,#{taskTitle},#{taskTime},#{taskStartTime, java} -->
<!-- 		) -->
	</insert>

</mapper>